spring:
    datasource:
        url: jdbc:mysql://localhost:3306/inventory
        username: root
        password:
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: false
        properties.hibernate.format_sql: true